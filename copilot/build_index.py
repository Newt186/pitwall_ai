"""  FUNCTION 1 — load_texts():
    - Loop through every .txt file in data/raw/
    - Read the contents of each file
    - Print the filename as you load it
    - Join all contents into one big string and return it"""
import os

def load_texts():
    all_text = ""

    folder_path = "data/raw"

    for file_name in os.listdir(folder_path):
        
    for x in 

